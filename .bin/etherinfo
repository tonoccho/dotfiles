#!/usr/bin/env zsh
source ${HOME}/.lib/netinfo

wifiStatus() {
  local etherStatus=`getEtherStatus`
  if [ "${etherStatus}" = "connected" ]
  then
    echo "\uf502 `getEtherIpAddress`"
  else
    echo "\uf701"
  fi
}

wifiStatus
